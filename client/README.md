# webrelease2-template-lsp-client

WebRelease2 Template Language Support の **VS Code 拡張（クライアント側）** です。

- `.wr2` / `.wr2t` を対象に、同梱の LSP サーバー（`server/out/server.js`）を起動して言語機能を提供します。
- **Python / pygls は不要** です（Node.js/TypeScript 実装です）。

拡張機能としての機能概要やインストール手順は、リポジトリ直下の `README.md` を参照してください。
